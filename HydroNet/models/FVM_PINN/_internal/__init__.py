# Internal FVM-PINN implementation. Ported from fvm_pinn_swe src/ verbatim
# (modulo import-path fixes). Not part of the public API — callers should
# use HydroNet.models.FVM_PINN.{model,trainer,data} instead.
